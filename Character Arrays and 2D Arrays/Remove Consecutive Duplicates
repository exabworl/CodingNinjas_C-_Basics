void removeConsecutiveDuplicates(char input[]) {
     // Write your code here
    int count=0,o=strlen(input);
    for(int j=0;j<o;j++)
    {
    for(int i=0;i<o-1;i++)
    {
        if(input[i]==input[i+1])
            input[i+1]=input[i+2];count++;
    }
    }
    input[o]=NULL;
   
}
